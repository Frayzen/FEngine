#pragma once

#include <glm/vec3.hpp>
#include <glm/vec2.hpp>

using namespace glm;

static const vec2 screenSize = vec2(2000, 1000);
static const vec3 bounds = vec3(10.0f, 9.0f, 0.0f);
static const vec2 size = uvec2(30, 30);
static const vec2 offset = vec2(0.2f, 0.2f);
static const float mass = 1.0f;
